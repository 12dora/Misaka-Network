node_modules/
dist/
build/
.env
.env.local
.credentials
# Runtime data dirs only — anchored so client/src/data/*.ts is never swallowed.
/data/
/server/data/
/client/data/
# Root has no package dependencies; a root lockfile would make `npm ci` a false success.
/package-lock.json
.DS_Store
*.log
.vscode/
.idea/
test-results/
playwright-report/
client/playwright-report/
client/coverage/
# Output of the deployment-base smoke build (client `npm run build:subpath`).
client/dist-subpath-smoke/
